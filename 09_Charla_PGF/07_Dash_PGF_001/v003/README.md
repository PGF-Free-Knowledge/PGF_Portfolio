# PGF Dashboard v003

Ejecutar desde la raíz del proyecto:

```powershell
python -m streamlit run app\app.py
```

El código carga el dataset y entrena el modelo ANTES de construir las pestañas.
No ejecutar con `python app.py`.
